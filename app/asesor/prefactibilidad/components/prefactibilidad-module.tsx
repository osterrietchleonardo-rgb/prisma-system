"use client";

// Placeholder mínimo de la Task 15: solo para que el menú y las páginas compilen.
// La pantalla real se implementa en la Task 16.
export function PrefactibilidadModule({ esDirector = false }: { esDirector?: boolean }) {
  return <div />;
}
